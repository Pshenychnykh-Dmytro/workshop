import { BaseFormPageProviderDirective } from './base-form-page-provider.directive';

describe('BaseFormPageProviderDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseFormPageProviderDirective();
    expect(directive).toBeTruthy();
  });
});
